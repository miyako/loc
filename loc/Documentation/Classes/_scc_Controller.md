# _scc_Controller : _loc_Controller: _CLI_Controller

`_scc_Controller` is an example subclass of `_CLI_Controller` for the `scc` class without a UI. 

The response is stored in `This.instance.data`.
